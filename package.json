{
  "name": "bsky-private-profile",
  "version": "1.0.0",
  "description": "Bluesky Private Profile Integration - Orchestrator",
  "private": true,
  "scripts": {
    "install:all": "pnpm install",
    "build:all": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "dev:app": "pnpm --filter bluesky-app dev",
    "dev:pds": "pnpm --filter @atproto/pds dev",
    "dev:lexicon": "pnpm --filter community-stream-lexicon dev",
    "dev:pinata": "pnpm --filter pinata-integration dev",
    "start:all": "concurrently -n app,pds,pinata -c cyan,magenta,green \"pnpm dev:app\" \"pnpm dev:pds\" \"pnpm dev:pinata\"",
    "setup": "node scripts/setup.js",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "keywords": [
    "bluesky",
    "atproto",
    "private-profiles",
    "ipfs",
    "pinata"
  ],
  "author": "CommunityStream",
  "license": "MIT"
}

